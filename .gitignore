.venv/
__pycache__/
*.pyc
.env
.DS_Store

#evn
.env
.env.*

# frontend
node_modules/
.next/
.turbo/
.vercel/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local architecture & change notes (not committed)
docs/

# tests
meal_plan_experiments/

# The admin API and admin web app live on the `admin` branch, not this one.
# If you switch branches inside one working copy these directories stay on disk
# (holding your local .env / venv); ignoring them here keeps `git status` honest
# and stops them being re-added to the user-main tree by accident.
adminbackend/
adminfrontend/
# Root-only: each frontend keeps its own lockfile (npm ci needs it).
/package-lock.json