.venv/
__pycache__/
*.pyc
.env
.DS_Store

#evn
.env
.env.*

# frontend
node_modules/
.next/
.turbo/
.vercel/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local architecture & change notes (not committed)
docs/

# tests
meal_plan_experiments/

# Local virtualenv used by adminbackend (the core backend uses .venv/).
venv/

# Root-only: each frontend keeps its own lockfile (npm ci needs it).
/package-lock.json